import { LearnPage } from '@/components/greenhub/LearnPage'

export const metadata = {
  title: 'Learn — GreenHub',
  description: 'New to cannabis? Learn what weed is, the difference between THC and CBD, indica vs sativa, consumption methods, and responsible use tips.',
}

export default function LearnRoute() {
  return <LearnPage />
}
