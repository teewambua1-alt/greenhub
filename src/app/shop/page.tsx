import { Shop } from '@/components/greenhub/Shop'

export const metadata = {
  title: 'Shop — GreenHub',
  description: 'Browse lab-tested cannabis products. Filter by strain, effect, and price.',
}

export default function ShopPage() {
  return <Shop />
}
